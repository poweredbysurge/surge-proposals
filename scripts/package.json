{
  "name": "surge-proposals",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'static site, no build needed'"
  },
  "devDependencies": {
    "dotenv": "^17.4.0",
    "stripe": "^22.0.0"
  }
}
